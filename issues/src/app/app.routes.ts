import { Routes } from '@angular/router';
import { IssueList } from './components/issue-list-component/issue-list.component';

export const routes: Routes = [
    { path: '', component: IssueList }
];
